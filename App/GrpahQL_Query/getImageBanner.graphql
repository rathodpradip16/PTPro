query getImageBanner{
    getImageBanner {
        result {
            id
            title
            description
            buttonLabel
            image
        }
        status
        errorMessage
    }
}
